{
  "name": "loop-saints",
  "version": "1.0.0",
  "private": true,
  "description": "Southampton FC StadiumLoop — matchday companion app delivered via NFC tap. Single-file HTML + assets, served via a tiny zero-dependency Node static server.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
